package com.backtoback.reseat.domain.reservation.service.concurrency.optimistic;
/**
 * 낙관적 락(Optimistic Lock) 기반 좌석 선점 동시성 테스트를 담는 패키지.
 * <p>T3-11(락 전략 3종 비교)에서 {@code OptimisticLockStrategy} 구현체와 재시도 정책 테스트가 함께 추가될 예정이다.
 * 현재는 구현체 착수 전이므로 빈 패키지로 유지한다.
 */
